apiVersion: v1
kind: Service
metadata:
  name: api-gateway
spec:
  type: LoadBalancer  # Meilleur choix pour l'exposition externe
  selector:
    app: api-gateway
  ports:
    - port: 80
      targetPort: 8080 